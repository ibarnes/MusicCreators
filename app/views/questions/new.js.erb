
$('#related').replaceWith("<%= escape_javascript(render :partial => 'headline', :locals => { :text => @text }) %>");

$("#msgbox").fadeTo(200,0.1,function() {
          $(this).html('This User name Already exists').addClass('messageboxerror').fadeTo(900,1);
        });

 $('#question_headline').focus(function(){$('#related').replaceWith('');});
